# Custom Providers

Put user-written data loaders here.

Use this when your data comes from:

```text
custom CSV structure
database query
API
Parquet
MongoDB
any other source
```

Your custom provider only needs to return the standard futures/options DataFrame columns.
