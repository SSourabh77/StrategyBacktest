# Config

Configuration files live here.

Use `backtest_config.json` by default.

Change these settings here:

```text
data provider
strategy name
execution mode
risk rules
report columns
```

Strategy-specific parameters and optimization grids live in:

```text
StrategyConfig/{strategy_name}/{strategy_name}_config.json
```
