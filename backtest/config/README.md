# Config

Configuration files live here.

Use `backtest_config.json` by default.

Change these settings here:

```text
data provider
strategy name
strategy parameters
execution mode
risk rules
optimization grid
report columns
```
