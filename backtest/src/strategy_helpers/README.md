# Strategy Helpers

Shared helper functions for strategy files.

Examples:

```text
common signal builder
RSI helper
shared indicator helpers
```

This keeps individual strategy files smaller without mixing them into one big library file.
